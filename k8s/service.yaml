apiVersion: v1
kind: Service
metadata:
  name: frisbee-api
spec:
  selector:
    app: frisbee-api
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000
      nodePort: 30000
  type: NodePort